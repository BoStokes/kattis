#include <bits/stdc++.h>
using namespace std;

typedef pair<int, int> ii;
typedef vector<int> vi;
typedef vector<ii> vii;

int main() {
    int N; scanf("%d", &N);
    vi distances(N);
    for (int i = 0; i < N; i++) scanf("%d", &distances[i]);
    sort(distances.begin(), distances.end());

    int M; scanf("%d", &M);
    vi holes(M);
    for (int i = 0; i < M; i++) scanf("%d", &holes[i]);




}
